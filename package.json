{
  "name": "local-weather-app",
  "version": "1.0.0",
  "description": "A local weather web application with dynamic backgrounds",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node test/test-weather-api.js",
    "prebuild": "./generate-version.sh",
    "docker:build": "npm run prebuild && docker build -t local-weather-app .",
    "docker:run": "docker run -p 8080:3000 local-weather-app",
    "docker:compose": "npm run prebuild && docker compose up --build",
    "docker:compose:down": "docker compose down",
    "docker:clean": "docker system prune -f && docker image prune -f"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "keywords": ["weather", "express", "nodejs", "local"],
  "author": "Local Weather App",
  "license": "MIT"
}